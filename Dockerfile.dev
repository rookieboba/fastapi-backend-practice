# 개발용 FastAPI Dockerfile
FROM python:3.11-slim

LABEL maintainer="rookieboba <terranbin@gmail.com>" \
    version="ver 25.04.08-dev" \
    description="FastAPI backend for development"

# 타임존 설정
RUN apt-get update && \
    apt-get install -y tzdata && \
    ln -fs /usr/share/zoneinfo/Asia/Seoul /etc/localtime && \
    dpkg-reconfigure -f noninteractive tzdata && \
    apt-get install -y curl netcat vim && \
    apt-get clean && rm -rf /var/lib/apt/lists/*

# 작업 디렉토리 설정
WORKDIR /app

# 의존성 설치
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# 소스 복사
COPY . .

# 개발용 entrypoint 설정
RUN chmod +x /app/sqlite3/docker-entrypoint.sh
ENTRYPOINT ["/app/sqlite3/docker-entrypoint.sh"]

# 개발 환경용 포트 오픈 및 앱 실행 (hot reload)
EXPOSE 8000
CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000", "--reload", "--reload-dir", "/app"]
